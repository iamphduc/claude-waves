---
name: code
description: Use when the user types /code or asks to execute a sprint via the orchestrator wave loop.
---

Dispatch the `orchestrator` subagent via the Agent tool, passing the user's args (if any) as the prompt.
