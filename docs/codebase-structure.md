# Codebase structure

> Stub — fill this in for your project. Keep it short and navigable: top-level layout, key modules, how the pieces fit.

The **`## Smoke recipe`** section below tells each engineer how to bring the app up for the runtime verification it runs in the browser before shipping (per `docs/engineer-protocol.md`). Fill it in — without it engineers can't browser-verify their slices and will cap their confidence at `medium`.

## Smoke recipe

- **Start commands:** <!-- web / api / worker / etc. -->
- **DB setup:** <!-- migrate + seed commands -->
- **Login credentials:** <!-- seeded test accounts, per role -->
- **Key URLs:** <!-- per shell / route -->
- **Verification:** <!-- the headless build/test command -->
