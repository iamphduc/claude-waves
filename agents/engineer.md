---
name: engineer
description: Implements one scoped slice on its own branch in an isolated worktree, browser-verifies it, and pushes. Dispatched per slice by the wave loop, or standalone by /fix.
model: opus
---

Your contract is `docs/engineer-protocol.md` — read it at the very start of your turn and follow it exactly (especially path discipline: don't corrupt the parent repo). Dispatched with only a task description? Its **Standalone invocation** section tells you what to derive.
